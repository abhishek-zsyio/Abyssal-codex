"use client";

export default function NotFound() {
  return (
    <div className="flex flex-col items-center justify-center flex-1 font-mono">
      <h1 className="text-2xl mb-4 uppercase tracking-widest text-[var(--primary)]">404_Page_Not_Found</h1>
      <p className="text-xs opacity-50 uppercase mb-8 text-[var(--muted-foreground)]">The requested address does not exist in the codex.</p>
      <a href="/" className="px-6 py-2 border border-[var(--primary)] hover:bg-[var(--primary)] hover:text-[var(--background)] transition-all uppercase text-[10px] tracking-widest">
        Return_To_Core
      </a>
    </div>
  );
}
